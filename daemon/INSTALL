You need:
   - Python >= 2.4
   - SQLObject

If yout don't have it, try running "easy_install sqlobject" as root.
If you don't have easy_install, go to the support folder and run "python ez_setup.py" then try again.


DATABASE CONFIGURATION
======================

Copy the file named "db.ini.dist" to "db.ini" and make the appropriate changes.
